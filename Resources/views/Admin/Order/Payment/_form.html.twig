<ul class="nav nav-tabs">
    <li class="active"><a href="#orde-payment-general" data-toggle="tab">{{ 'ekyna_core.field.general'|trans }}</a></li>
</ul>
<div class="panel-body form-body">
    {{ bootstrap_set_style('horizontal') }}
    {{ bootstrap_set_col_size('md') }}
    <div class="tab-content">
        <div class="tab-pane active" id="orde-payment-general">
            {{ form_row(form.amount) }}
            {{ form_row(form.method) }}
            {% if form.notes %}
                {{ form_row(form.notes) }}
            {% endif %}
            {#{{ form_widget(form) }}#}
        </div>
    </div>
    {{ form_rest(form) }}
</div>