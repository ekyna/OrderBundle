{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
    <h1>{{ 'ekyna_order.payment.header.remove'|trans|raw }}</h1>
{% endblock title %}

{% block actions %}
    {{ admin_resource_btn(order, 'show', {'label': 'ekyna_core.button.back'}) }}
{% endblock actions %}

{% block main %}
    {{ form_start(form) }}
    <div class="form-body">
        {{ bootstrap_set_style('horizontal') }}
        {{ bootstrap_set_col_size('md') }}
        {{ form_widget(form) }}
    </div>
    {{ form_end(form) }}
{% endblock main %}

